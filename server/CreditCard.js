const express = require("express");
const fs = require("fs");
const cors = require("cors");
const path = require("path");

const app = express();
const PORT = 5002;

app.use(cors)
app.use(express.json)

app.get(req, res)

const CreditCard = path.join(__dirname, )



